<%= form_for(@dbtable) do |f| %>
  <% if @dbtable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dbtable.errors.count, "error") %> prohibited this dbtable from being saved:</h2>

      <ul>
      <% @dbtable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :dbname %><br>
    <%= f.text_field :dbname %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :tabletype %><br>
    <%= f.number_field :tabletype %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
